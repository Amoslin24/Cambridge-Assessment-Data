# Cambridge 成绩分析面板 —— 老师 3 分钟上手指南

> 本工具完全在本地浏览器运行，不联网、不上传学生数据。建议使用 Chrome / Edge 最新版本。

## 一、准备 Excel/CSV 模板

- **推荐做法**：直接使用系统提供的模板，避免字段错误。
- 在导入首页（`http://localhost:3200/`）中部，点击：
  - `一键导出中文空模板`
  - 或 `一键导出英文空模板`
- 也可以点击页面底部的：
  - `下载标准模板（CSV）`
  - `下载中文模板（CSV）`
  - `下载示例数据（CSV）`
  - `下载异常样例（CSV）`

### 必填字段与要求

- **Name / 姓名**：支持中文、英文，或“中文 英文”组合。
- **Class / 班级**：如 `G4A`。
- **Set / 组别**：如 `Set 1`，没有可留空。
- **Level / 级别**：必须是以下之一：
  - `Starters`、`Movers`、`Flyers`、`KET`、`PET`、`FCE`
- **ExamDate / 考试日期**：
  - 建议填写 `YYYY-MM-DD`，如 `2026-03-15`
  - 也可在 Excel 中使用正常日期单元格，系统会自动转换。
- **原子分项分数**（每格填该 Part **答对题数/得分**，为 **非负整数**；上限随级别与题段变化，与 Cambridge 官方试卷一致）：
  - 阅读：`R_P1` ~ `R_P7`
  - 听力：`L_P1` ~ `L_P5`（**Starters、PET、FCE 仅 4 段听力**，`L_P5` 请留空或填 0；若误填超出该段满分会提示并封顶）
  - 写作（仅 KET/PET/FCE）：`W_P1` ~ `W_P2`（KET 各 0–15；PET/FCE 各 0–20）
  - 各级别各 `R_*` / `L_*` 的满分对照表见导入页可下载说明 **`public/templates/cambridge-import-guide.md`**（或首页「模板说明」链接）。**不得**再按「所有阅读听力格都是 0–5」理解。
  - Starters/Movers/Flyers 的 Writing 已并入 Reading，仅填写 `R_P*` 即可，`W_P1/W_P2` 可以保持为空或 0。

## 二、导入与追加数据

1. 打开导入首页 `http://localhost:3200/`。
2. 在顶部点击 `选择文件`，选择你的 Excel 或 CSV。
3. 根据需要选择导入模式：
   - **覆盖导入**：清空原有数据，仅保留本次文件内容。
   - **追加导入（自动去重）**：
     - 以 `Name + ExamDate + Level + Class + Set` 作为去重键。
     - 如遇到重复键，系统会先给出“将被覆盖的记录预览”，需要老师确认后才会生效。

> 提示：如果导入前检查不通过（缺列、级别错误、日期解析失败、小题分值越界等），页面会在“解析异常”区域给出详细行号与问题描述，请根据提示修正后再导入。

## 三、基础筛选与核对

导入完成后，页面中部的“数据筛选”与“换算核对面板”可帮助老师快速检查数据：

- 可按 **级别 / 班级 / 日期 / 姓名关键词** 进行筛选。
- 勾选“仅看异常行”可以只关注有问题的记录。
- “换算核对面板”会显示：
  - 每名学生的 **原始分**（R/R&W、W、L）
  - 对应的 **换算分/盾牌**
  - 用于与官方 Excel 换算表快速交叉验证。

如发现模版或录入错误，可：
- 修改原始 Excel，再次 **覆盖导入**；
- 或清空本地数据重新导入（见下文）。

## 四、分析页使用（趋势与画像）

1. 在导入首页顶部点击 `进入数据分析页`，或直接访问 `http://localhost:3200/analysis`。
2. 在分析页顶部选择：
   - 学生（或保留“全部学生”做整体观察）
   - 级别 / 班级 / 组别
   - 日期范围（可使用“最近30天 / 最近90天 / 最近一次考试周 / 本学期”等快捷按钮）
3. 主要模块说明：
   - **班级/级别分布图（换算总分）**：查看同一筛选条件下学生的整体水平分布。
   - **学生画像卡**：选定某一学生后，展示最近一次考试的：
     - 换算总分、原始总分
     - 分技能结构（YLE：R&W / L；KET/PET：R/W/L；**FCE**：Reading / Use of English / W / L 四项，与官方分项对应）
     - 薄弱技能、薄弱小题（便于布置针对性练习）。
   - **个人多次考试趋势**：查看同一学生的多次考试总分走势。
   - **分技能对比与小题对比**：可多选考试日期，按技能与小题维度观察变化。

> 对 YLE 级别（Starters/Movers/Flyers），分析页的分技能柱状图 Y 轴以**盾数**展示，并在 3 盾、5 盾处给出参考线，便于判断达标与优秀水平。

## 五、结果导出与本地备份

在导入首页“结果导出”区域，老师可以：

- `导出当前解析结果`：
  - 导出包含所有学生记录（含小题分数、换算结果）的 CSV，用于留档或进一步加工。
- `导出异常行`：
  - 导出当前所有解析异常的行，方便集中修改。
- `清空本地数据`：
  - 清除当前浏览器中所有成绩与异常记录（不会影响原始 Excel）。
- `导出本地完整备份（JSON）`：
  - 一键导出本地状态（成绩记录 + 异常信息 + 导入审计），建议定期备份。
- `从备份恢复本地状态`：
  - 选择之前导出的 JSON 文件，即可恢复当时的全部数据与导入记录。

## 六、常见问题

1. **导入时报错“考试日期无法解析”怎么办？**
   - 确认日期列为正常日期格式或 `YYYY-MM-DD` 文本。
   - 避免使用诸如 `3月15日`、`2026/3/15` 这类混合格式。

2. **导入时报“小题分值越界”怎么办？**
   - 查看异常说明中的 **「超过当前级别上限」** 提示：该格在该级别下的满分不是固定 5，请对照 `cambridge-import-guide.md` 中的表格。
   - 若为填涂错误，请按实际答对题数（或该 Part 得分）重新填写 **0 到该 Part 满分之间** 的整数。

3. **Starters/Movers/Flyers 需要单独填 Writing 吗？**
   - 不需要。Writing 已并入 Reading，R 列即 R&W。
   - 仅 KET/PET/FCE 需要填写 `W_P1`、`W_P2`。

## 七、演示数据包建议用法

- **标准样例（`cambridge-import-sample.csv`）**：用于演示完整主流程（导入成功、分析页趋势、学生画像导出）。
- **异常样例（`cambridge-import-sample-anomalies.csv`）**：用于演示异常治理能力，覆盖：
  - 日期不可解析
  - 小题分值越界
  - 非数字分值
  - 非受支持级别
  - 关键字段缺失（姓名为空）
- 建议演示顺序：
  1. 先导入标准样例，确认基础图表与换算结果。
  2. 再以覆盖模式导入异常样例，展示“解析异常”区域与行级问题提示。
  3. 导出“异常行 CSV”给老师示范复核与回填流程。

## 八、只让一位同事试用（不想配局域网时）

本产品是 **Next.js 网站**，没有「双击 exe、完全零安装」的官方安装包；**同事那边至少要能打开一个网页地址**。下面按「省事程度」排列。

### 方案 1：当面或会议里你看屏演示（零网络配置）

你本机 `npm run dev` 或 `npm run start`，用 **屏幕共享**（腾讯会议、飞书等）带同事走一遍导入与分析。同事**不装任何东西**，也**不配 IP**，适合「先看懂产品」。

### 方案 2：同事自己本机跑（适合会一点终端的同事）

同事安装 **Node.js LTS**，克隆仓库，`npm install` → `npm run dev`，浏览器打开 `http://localhost:3000`。数据在他自己电脑上，与你不互通。

### 方案 3：远程一条链接（你本机开着服务 + 内网穿透）

1. 你在本机照常启动网站（例如端口 `3207`）。  
2. 使用 **内网穿透** 工具（常见如 [ngrok](https://ngrok.com/)、[Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/)），把本机端口映射成一个 **https 开头的临时网址**。  
3. 把该网址发给同事，他**用浏览器打开即可**操作，**不必**和你同一 Wi‑Fi、**不必**查局域网 IP。

**重要**：隧道网址会经过公网中转，**请勿**用含真实学生敏感信息的正式成绩表做试验；试用请用本仓库自带的 **`cambridge-import-sample.csv`** 等假数据。

---

**结论**：没有「完全不用任何人跑 Node、又远程能点你这台电脑上的同一套数据」的官方方案；**对一位同事最省事**通常是 **方案 1（看演示）** 或 **方案 3（一条链接 + 假数据）**。

如有新需求（例如新增指标、特殊班级统计口径等），可以基于本指南直接补充相应模块，无需改变上述基础操作流程。

